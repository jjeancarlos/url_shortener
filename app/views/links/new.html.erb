<div class="w-full max-w-lg mx-auto backdrop-blur-xl bg-white/10 border border-white/20 p-8 rounded-2xl shadow-2xl">
  <h1 class="text-2xl font-bold mb-6 text-white text-center">Criar Novo Link</h1>
  
  <%= form_with model: @link, class: "space-y-6" do |f| %>
    <div>
      <%= f.label :original_url, "Cole sua URL longa aqui", class: "block text-sm font-medium text-gray-300 mb-2 ml-1" %>
      <div class="relative">
        <%= f.text_field :original_url, class: "w-full bg-slate-800/50 border border-white/10 rounded-xl p-4 text-white placeholder-gray-500 focus:ring-2 focus:ring-emerald-500 focus:border-transparent transition-all outline-none pl-4", placeholder: "https://exemplo.com/pagina-muito-longa" %>
      </div>
    </div>
    
    <%= f.submit "Encurtar URL", class: "w-full bg-gradient-to-r from-emerald-500 to-teal-500 text-white font-bold py-3.5 rounded-xl shadow-lg hover:shadow-emerald-500/20 hover:scale-[1.01] transition-all cursor-pointer" %>
  <% end %>

  <div class="mt-6 text-center">
    <%= link_to "← Voltar para meus links", links_path, class: "text-gray-400 hover:text-white text-sm transition-colors" %>
  </div>
</div>